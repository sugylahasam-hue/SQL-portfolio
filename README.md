# SQL Portfolio Project

Проект-портфолио, посвящён изучению и применению языка **SQL** для обработки данных и аналитики.

Здесь собраны упражнения и мини-кейсы, в которых демонстрируется работа с базами данных: создание таблиц, запросы, агрегации, джойны и построение отчётов.

---

## 📘 Навыки

- Создание и модификация таблиц (`CREATE TABLE`, `ALTER TABLE`)
- Вставка, обновление и удаление данных (`INSERT`, `UPDATE`, `DELETE`)
- Выборки данных (`SELECT`), фильтрация (`WHERE`), сортировка (`ORDER BY`)
- Группировка и агрегатные функции (`GROUP BY`, `HAVING`)
- Объединение таблиц (`JOIN` — `INNER`, `LEFT`, `RIGHT`, `FULL`)
- Подзапросы и CTE (`WITH`)
- Работа с оконными функциями
- Создание аналитических запросов / отчётов

---

## 📂 Структура проекта

| Файл/папка | Описание |
|-----------|----------|
| `sql_exercises.sql` | Основной файл с упражнениями и примерами |
| `database_schema/` | Скрипты создания и заполнения баз данных (если есть) |
| `results/` *(опционально)* | Выгрузка результатов запросов / отчёт в виде CSV или PDF |

---

## 🛠️ Инструменты

| Технология | Назначение |
|-----------|-----------|
| SQL | Язык для управления и анализа данных |
| SQLite / PostgreSQL / MySQL | СУБД, с которой работал(а) (укажи свою) |
| Jupyter Notebook + SQL-Kernel / VS Code | Среда разработки (если использовал(а)) |

---

## 🚀 Как запустить

1. Импортируйте схему базы данных (если есть) в выбранную СУБД:

```sql
-- Пример: SQLite
\. schema.sql
```

2. Запустите файл с запросами `sql_exercises.sql` или откройте в редакторе.

3. Если есть результаты, просмотрите папку `results/`.

---

## 🎯 Цель проекта

Развить навык аналитики с помощью SQL, повысить уверенность в написании сложных запросов и подготовиться к задачам по обработке данных в настоящих проектах.
